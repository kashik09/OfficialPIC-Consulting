{
  "name": "official-pic-consulting",
  "version": "1.0.0",
  "description": "Using JSON Server instead of MongoDB",
  "scripts": {
    "start:json": "json-server --watch db.json --port 5000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
